<h2><%=current_user.name %></h2>

<h4><%=current_user.email %></h4>

<% #add profile edit functions, location, list of listings, add listing, etc. %>

<%= link_to "Home", root_url, :class => "btn btn-default" %>
